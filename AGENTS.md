# BIQA_SVR 项目约定

## 目标与范围

- 复现 Hu 等人的 BCQI：Toward a No-Reference Quality Metric for Camera-Captured Images，DOI：10.1109/TCYB.2021.3128023。
- 当前仅研究 LIVEC（LIVE In the Wild Image Quality Challenge Database）；CID2013 和跨数据库实验不属于当前任务范围。
- 主线为低层感知特征 + 预训练网络高层语义特征 + RBF SVR。Python 实现属于本项目复现，不能宣称是作者官方实现。
- 论文给出的代码入口为 https://github.com/YT2015?tab=repositories 。没有取得并核验 BCQI 源码前，不臆造作者实现细节。

## 环境与执行

- 目标环境为 `environment.yml` 定义的独立 Conda 环境 `BIQA_SVR`，Python 3.11。环境定义不代表全部依赖已验证。
- 执行项目代码前先核验该环境。优先显式调用其 `python.exe`；本机 Miniforge 下路径为 `E:\Miniforge\envs\BIQA_SVR\python.exe`，必须检查存在性，不依赖裸 `python`。
- 不使用或修改 base、vit、mamba 等无关环境。目标环境不存在时明确说明；桌面内置 Python 仅可用于临时检查，不能当作正式训练验证。
- 运行依赖放在 `requirements.txt`，测试依赖放在 `requirements-dev.txt`。当前版本范围是初始规格，不是已验证锁文件；首次正式跑通后记录实际版本和 torch/torchvision 配对。
- 下载前检查本机网络与代理；不擅自改变全局源。安装或更新依赖应属于用户授权的任务范围。

## 数据约定

- 默认根目录：`data/ChallengeDB_release/`；图像位于 `Images/`，标注位于 `Data/`。
- `AllImages_release.mat`、`AllMOS_release.mat`、`AllStdDev_release.mat` 各包含 1169 项，其中包含 7 张主观实验练习图。正式 LIVEC 实验应使用 1162 张。
- 根据标注文件中的名称建立图像与 MOS 对应关系，再用同一掩码排除 `trainingImages` 中的 `t1.bmp` 至 `t7.bmp`。不要独立排序图像和分数，不要仅凭遍历顺序对齐。
- `trainingImages` 是受试者练习图目录，不是模型训练集。忽略 `__MACOSX`、`._*`、`.DS_Store` 等元数据文件。
- 原始数据只读；不重新压缩、覆盖或全局缩放原图。区分低层特征的原图处理与高层网络输入预处理。
- 数据入口检查样本数、路径、重复项、MOS 有限性与对应关系；无法解码的图像应报错，不静默丢弃。

## 复现与评估

- 论文主实验按图像随机划分 80% 训练、20% 测试，重复 1000 次并汇报均值。Python 默认可采用 929/233，记录取整方式；没有作者代码证据时不得称其为作者精确划分。
- 固定并保存随机种子和每轮图像 ID。所有比较方法使用相同划分；小规模调试结果必须标注实际轮数。
- 标准化、特征选择、SVR 调参仅在训练数据内完成；需要验证时从训练集划分或做内部交叉验证，禁止利用测试集选择超参数。
- 论文语义特征示例为 SqueezeNet 的 227×227 中心裁剪和 softmax 前全局池化的 1000 维输出。核对网络版本、权重、输入归一化与具体取层；不能以任意分类概率或默认 resize 替代而不说明。
- 六类低层属性为亮度、饱和度、对比度、噪声、锐度、自然度。颜色空间、像素尺度、边界处理、滤波器与数值估计必须逐项对照论文。
- 保存原始预测和 MOS，分别记录原始与五参数 logistic 映射后的评估。论文 III-A 描述对映射后分数计算指标；不要悄悄改用其他 IQA 协议。
- 测试集 logistic 拟合若用于论文报告，应明确其为事后评估映射，不作为部署校准或训练步骤。不能将该拟合反馈给模型选择。
- 主要指标为 SRCC、KRCC、PLCC、RMSE；完整对应表 II 时还需要 PWRC。论文未明确的参数应记录为本项目选择。
- 特征缓存要绑定图像 ID、预处理、网络权重和提取器版本；验证一致性后再复用。

## 文件与验证

- 数据保留在 `data/`，生成特征在 `features/`，权重在 `checkpoints/`，实验产物在 `outputs/`，临时文件在 `tmp/`。按实际需要创建目录，不预建空壳模块。
- 原始数据、缓存、模型和下载的第三方源码不入 Git；小型可复现配置与测试可入 Git。保留外部代码的来源与许可，不擅自赋予其新许可证。
- 用 pathlib 管理路径；公共接口提供类型注解。新增依赖、目录或重构必须服务当前任务。
- 核心实现后验证标签对齐、划分无交集、特征维度/有限性、训练与推理以及异常输入。尚无代码或测试时，不报告训练或测试通过。
- 未经用户要求不提交、不推送、不创建分支或 PR；保留用户已有变更。

# BCQI、BRISQUE 与 NIQE 在 LIVEC 上的实验报告

记录日期：2026-09-24。本文记录本项目**已经生成并核验的产物**，研究范围仅为 LIVEC。参考文献为 Hu 等人的 *Toward a No-Reference Quality Metric for Camera-Captured Images*（[DOI: 10.1109/TCYB.2021.3128023](https://doi.org/10.1109/TCYB.2021.3128023)）；论文表 II、表 III 的数值取自本地 PDF 第 3658 页。以下 Python 实现不是作者官方源码。

## 实验设置

LIVEC 标注文件含 1169 项，排除 7 张主观实验练习图后，使用 1162 张正式图像及其 MOS。BCQI 从同一批图像中按每轮随机种子划分 929 张训练图和 233 张测试图（`test_size=0.2`，测试数向上取整）；1000 轮种子为 42～1041。每轮独立拟合标准化器、在当轮训练集内部交叉验证 SVR 参数，并训练 RBF SVR；前一轮模型不传给后一轮。1000 轮的目的在于估计方法的平均测试表现，不是从 1000 个模型中挑选测试误差最小者。

本项目 BCQI 使用 7 维低层属性与预训练 SqueezeNet 的 1000 维语义特征，共 1007 维。比较时，BRISQUE 与 NIQE 都在相同的 1162 张原图上各计算一次分数，并按 BCQI 每轮保存的**相同测试图像 ID**取出其中 233 个分数。基线缓存与 BCQI 特征缓存核对图像名称、MOS、图像哈希和来源缓存哈希。两种基线原始分数越低越好；计算相关系数前将其取负，统一为越高越好。

| 方法 | 本项目实现 | LIVEC MOS 参与该方法训练 | 当前评估轮数 |
| --- | --- | --- | ---: |
| BCQI | 低层 + SqueezeNet 特征，训练集内部调参的 RBF SVR | 每轮只用当轮训练集 | 1000 |
| BRISQUE | PIQ 0.8.0 的公开预训练模型，原始 RGB 全图输入 | 本项目不重新训练；预训练数据来源不能由当前产物核实 | 1000 |
| NIQE | scikit-video 1.3.0，原始 RGB 全图转 Pillow 8-bit 灰度后输入 | 不使用 LIVEC MOS 训练 | 50 轮试跑；1162 张的分数已全部缓存 |

每轮对测试预测和测试 MOS 单独拟合五参数 logistic 曲线，**仅用于事后指标报告**，不参与训练、超参数选择或部署预测。主表采用映射后 SRCC、KRCC、PLCC、RMSE 的逐轮均值；SRCC、KRCC、PLCC 越高越好，RMSE 越低越好。原始分数的结果也保存在实验产物中。当前尚未计算论文表 II 的 PWRC。

## 三种方法的同划分试跑：前 50 轮

下表仅取种子 42～91 对应的**同一批 50 组测试集**。已经核对 BRISQUE 与 NIQE 报告中的每轮种子和测试图像名称完全一致；BCQI 指标也取这 50 轮。因此表内三行可以横向比较，但这张表**不是 1000 轮正式均值**。

| 方法 | SRCC ↑ | KRCC ↑ | PLCC ↑ | RMSE ↓ |
| --- | ---: | ---: | ---: | ---: |
| BCQI | 0.7377 | 0.5412 | 0.7695 | 12.8717 |
| BRISQUE | 0.3096 | 0.2099 | 0.3706 | 18.7542 |
| NIQE | 0.4546 | 0.3096 | 0.5022 | 17.4677 |

这 50 轮中，BCQI 的三项相关系数均高于两种基线，RMSE 也更低；本项目 NIQE 的指标高于预训练 BRISQUE。NIQE 的 50 轮比较报告标有 `debug=true`，不能把该行写成论文式的 1000 轮结果。

## 已完成的 1000 轮结果

BCQI 和 BRISQUE 已在全部 1000 组相同测试划分上完成比较。下表同样采用各轮五参数 logistic 映射后的指标均值；四项指标的有效轮数均为 1000。

| 方法 | SRCC ↑ | KRCC ↑ | PLCC ↑ | RMSE ↓ |
| --- | ---: | ---: | ---: | ---: |
| BCQI | 0.7332 | 0.5387 | 0.7647 | 12.9927 |
| BRISQUE | 0.3143 | 0.2132 | 0.3768 | 18.6894 |

本项目对**同一轮、同一图像**的映射后绝对残差执行双侧配对 t 检验，显著性水平为 0.05；若同一报告含多个基线，按轮进行 Holm 校正。当前两个基线是分别生成报告的，因此上述报告没有完成三方法合并后的多重比较。BCQI 相对 BRISQUE 的判定为 1000/1000 轮“BCQI 更优”，相对 NIQE 的试跑判定为 50/50 轮“BCQI 更优”。各轮测试图像会重复出现，**不能**把这些轮次当成互相独立的 1000 或 50 个样本合并成单一显著性检验；论文表 III 的具体残差检验细节也未完全公开，本项目的判定不等同于原表 III 的精确复现。

## 与论文表 II 的关系

下表为**论文原文在 LIVEC 上报告的数值**，并非本项目测量值。

| 论文方法 | SRCC ↑ | KRCC ↑ | PWRC ↑ | PLCC ↑ | RMSE ↓ |
| --- | ---: | ---: | ---: | ---: | ---: |
| BCQI | 0.7506 | 0.5542 | 10.2320 | 0.7732 | 12.7866 |
| BRISQUE | 0.5228 | 0.3639 | 6.5182 | 0.5610 | 16.6950 |
| NIQE | 0.4495 | 0.3073 | 6.2348 | 0.5068 | 17.3911 |

论文与本项目都呈现 BCQI 优于 BRISQUE、NIQE 的方向，但**基线之间的排序不同**：论文表 II 的 BRISQUE 优于 NIQE；本项目同划分 50 轮中，NIQE 优于 PIQ 预训练 BRISQUE。关键限制是本项目没有按每轮训练集重新训练/校准 BRISQUE，预训练权重与论文表 II 的实现不等价；NIQE 的灰度转换及软件版本也属于本项目已记录的选择。BCQI 的作者源码未取得，网络权重、预处理和数值细节无法确认完全一致。因此，不能用表中的接近程度证明精确复现，也不能将本项目 BRISQUE 的较低分数解释为论文结论错误。

## 产物与复核入口

| 内容 | 项目内路径 | 状态 |
| --- | --- | --- |
| BCQI 1000 轮配置、逐轮预测和汇总 | `outputs/svr_1000_logistic_fixed/` | 已完成；由原 1000 轮结果修复 38 轮失败的事后 logistic 映射，未重训 SVR |
| BRISQUE 全图分数 | `features/livec_brisque_piq_v1.npz` | 1162 张已缓存 |
| BRISQUE 1000 轮比较 | `outputs/compare_brisque.json` | 已完成 |
| NIQE 全图分数 | `features/livec_niqe_skvideo_v1.npz` | 1162 张已缓存，分数均为有限值 |
| NIQE 前 50 轮比较 | `outputs/compare_niqe_50_debug.json` | 已完成，仅为试跑 |
| 实现与运行方式 | `compare_baselines.py`、`README.md`、`requirements-baselines.txt` | 已记录版本、输入处理和命令 |

目前选择**只比较 BCQI、BRISQUE、NIQE 三种方法**，不扩展到论文表 II 的全部方法。若后续需要一张三种方法均为 1000 轮的正式结果表，NIQE 只需复用现有分数缓存补跑逐轮指标和 logistic 拟合，**无需重新提取图像分数或重新训练 BCQI**；在此之前，三方法共同结论应明确标注为 50 轮探索性结果。

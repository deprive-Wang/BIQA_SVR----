# 本项目 Python 复现以 Python 3.11 为基础。
# 以下版本范围不是经验证的锁定版本，也不是作者原始环境。
numpy>=1.26,<3
scipy>=1.11,<2
Pillow>=10,<13
scikit-learn>=1.4,<2
PyWavelets>=1.5,<2
matplotlib>=3.8,<4
torch>=2.2,<3
torchvision>=0.17,<1
# 由 pip 解析兼容的 torch/torchvision 版本组合。
# GPU 构建需按目标机器情况，参照 PyTorch 官方说明另行选择。
